const Header = () => {
    return (
        <div className="app-header">
            <h1>Notes List</h1>
        </div>
    )
}

export default Header;

// all contents of the component needs to be 
//inside a parent/single div